name: Delete merged short-game branch once

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/delete-short-game-foundation-branch-once.yml

permissions:
  contents: write

jobs:
  delete-branch:
    runs-on: ubuntu-latest
    steps:
      - name: Delete merged branch and verify
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const branch = 'agent/issue-24-short-game-foundation';

            try {
              await github.rest.git.deleteRef({
                owner,
                repo,
                ref: `heads/${branch}`
              });
              core.info(`deleted: ${branch}`);
            } catch (error) {
              if (error.status !== 404) {
                throw error;
              }
              core.info(`already absent: ${branch}`);
            }

            try {
              await github.rest.git.getRef({
                owner,
                repo,
                ref: `heads/${branch}`
              });
              core.setFailed(`branch still exists: ${branch}`);
            } catch (error) {
              if (error.status !== 404) {
                throw error;
              }
              core.info(`verified absent: ${branch}`);
            }
