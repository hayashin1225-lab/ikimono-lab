# 設計判断記録

## 記録規則

重要な判断は、判断ID、日付、状態、対象、原発話または問題、判断、理由、代替案、副作用、実装、検証結果、得られた原則、関連Issue、関連PR、Figma参照を記録する。日付はGit履歴などで正確に確認できる場合だけ記載し、それ以外は「確認要」とする。

## DEC-GUI-001：結果画面の固定操作バー

- **日付：** 確認要
- **状態：** 実装済み・公開確認済み
- **対象：** 結果画面の固定操作バー
- **判断：** 結果画面の主要操作を固定表示する。
- **理由：** 再抽選までのスクロール負荷を減らす。
- **失敗：** 旧ルールの `top: 8px` を解除せず、`bottom: 0` と同時適用した。
- **結果：** iPhone Safariで操作バーが画面高へ伸びた。
- **代替案：** 固定高さやJavaScript計算は採用せず、競合した制約だけを解除する。
- **実装：** `top: auto` を明示した。
- **検証結果：** ローカル分離環境で複数幅の対象結果画面と最終computed styleを確認した。PR #11をマージし、GitHub Pagesへの反映を確認した。人間が公開版をiPhone Safari実機で確認し、「良いと思う」と評価した。
- **得られた原則：** 固定要素は `top`、`bottom`、`height`、`inset` を合成して確認する。computed styleを確認し、対象状態を実ブラウザで表示し、公開後に実機確認する。
- **関連Issue：** 未作成
- **関連PR：** PR #8、PR #11
- **Figma参照：** 未作成

関連CSSを将来変更する場合は、対象状態での回帰確認を行う。

以後の判断はこの形式で追記する。

## DEC-RUNTIME-001：連絡将校MVPによる実行統制

- **日付：** 確認要
- **状態：** 実装済み・初回試行未実施
- **対象：** AIオーケストレーション.runtimeの連絡将校MVP
- **問題：** 人間がChatGPTとCodexの間で、情報運搬、起動、状態確認、成果物回収を毎回担っていた。
- **判断：** Windows PowerShellを用いた連絡将校MVPを追加し、承認済みIssue一件の実行統制を外在化する。
- **理由：** 既存のWindows・Git・GitHub CLI・Codex CLIと整合し、外部PowerShellモジュールなしでログ、排他、プロセス、GitHub状態を扱えるため。
- **代替案：** GitHub Actions、常駐アプリ、複数AIアダプター、分散ロックは、権限・運用・失敗復旧の検証前には採用しない。
- **実装：** GitHubラベル起点、ローカルFileStream lock、固定Issueスナップショット、Codex Worker、差分検査、連絡将校によるcommit・push・PR作成を分離する。
- **検証結果：** PowerShell構文、JSON、ignore、SelfTest、DryRun、プロセスランナーを確認対象とする。初回実Issue試行とScheduled登録は未実施である。
- **得られた原則：** 意味判断は人間とChatGPTへ残し、CodexのGit・GitHub操作を実行統制から分離する。一回に一件だけ処理し、自動マージ・自動公開・自動再試行を導入しない。
- **関連Issue：** Issue #13
- **関連PR：** 確認要
- **Figma参照：** 該当なし
